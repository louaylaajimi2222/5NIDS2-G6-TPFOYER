global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'backend'
    static_configs:
      - targets: ['app:8089']  # Modifiez le port selon votre configuration

  - job_name: 'database'
    static_configs:
      - targets: ['bd:3306']  # Modifiez le port selon votre configuration
